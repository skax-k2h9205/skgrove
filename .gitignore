# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
out/
.next/
.turbo/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Misc
.cache/
coverage/
.vercel
*.local
.env*
# 값이 없는 설정 템플릿은 저장소에 있어야 한다. .env* 가 이것까지 잡아
# 새 예시 파일이 조용히 빠지고 있었다(.env.ai.example 등 기존 3개는 강제 추가된 상태).
!.env*.example

# 실명·사번·이메일이 담긴 dev 시드 스크립트. 절대 커밋 금지(로컬 전용).
scripts/seed-real-profiles.mjs
scripts/set-account-photos.mjs

# 회식 자리배치 임시 도구(로컬 전용). 실명 명단과 손으로 받은 성별이 파일에 박혀 있고,
# public/ 은 배포되면 URL 만으로 열리므로 공개 저장소에 올리지 않는다.
# 앱 안의 자리배치(커넥셔너 전용, features/seating)가 정식 경로다.
public/seating.html
pims_login_tmp.mjs

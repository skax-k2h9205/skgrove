# SK Grove — 남은 일과 현재 상태

다른 PC에서 이어서 작업하기 위한 인계 문서. 디자인 판단 기준은 `톤앤매너-디자인.md`에
따로 있다.

---

## 시작하기

```bash
npm install && npm run dev
```

`http://127.0.0.1:5173` — 비밀번호 없이 **이름 + 사내메일**이 일치하면 로그인된다.

| 이름 | 사내메일 | 권한 | 보이는 메뉴 |
|---|---|---|---|
| 이선민 | `sunmin.l@sk.com` | 팀리더 | 14개 |
| 이두민 | `dumin@sk.com` | 팀원 | 12개 |
| 김승현 | `k2h9205@sk.com` | 파트리더 | 13개 |
| 김수정 | `crystalk@sk.com` | 팀원 | 12개 |

마이페이지는 사이드바에 없다. **헤더 우측 사용자 칩**을 누르면 들어간다.

---

## 현재 상태

- 브랜치 `dev` — `origin/dev` 기준. 다른 PC에서 `git pull` 하면 된다.
- 중간에 올라온 SKSOOP-152·153(캔미팅 DB 영속화, 파트 열 정렬)을 머지했다.
  `styles.css`의 `.can-part-columns` 한 곳만 충돌했고, **상대의 기능(파트 개수만큼
  등분)에 이쪽 토큰(`var(--space-3)`)을 얹어** 해결했다. 브라우저에서 3파트 → 3열,
  빈 칸 없음을 확인했다.
- `npm test` **237/237**, `npm run build`, `tsc --noEmit` 모두 통과
- 두 역할 × 두 폭(1280px / 390px) 접근성 결함 **0**
- 하드코딩 색상값 **0** / 정의 없는 `var()` **0**

### 새로 붙은 기능 — 모임 · 번개

**한때 '번개 모임'과 '일정 공모' 두 메뉴였다가 하나로 합쳤다.**
규칙(`gatheringRules.ts`, `aiPoster.ts`)에 `kind` 분기가 단 하나도 없었기 때문이다.
실제로 다른 건 등록할 때 **기본 날짜(오늘 vs 일주일 뒤)와 날짜 칩 노출** 둘뿐이었다.

그 정도 차이로 메뉴를 나누면 "이건 번개인가 공모인가"를 등록자가 판단해야 하고,
보는 쪽은 두 메뉴를 다 열어야 놓치지 않는다. `kind` 는 **등록 폼의 첫 선택**으로
내렸고, 고르면 기본 날짜와 칩이 따라 바뀐다.

> `kind` 는 저장되지만 피드·상세 어디에도 표시하지 않는다. 표시할 만한 차이가
> 아니기 때문이다 — 다음 주로 잡은 '번개'와 '공모'는 구별되지 않는다.
> 실제로 의미 있는 신호는 포스터 아래 "6시간 뒤 / 3일 뒤"가 이미 나르고 있다.
> 나중에 필터가 필요해지면 그때 붙이면 된다.

| 파일 | 역할 |
|---|---|
| `src/gatheringRules.ts` | 선착순·대기 승계·상태 파생. 순수 함수, 28 테스트 |
| `src/aiPoster.ts` | 포스터 문구/색/아이콘. 엔드포인트 없으면 로컬 폴백, 9 테스트 |
| `src/gatheringStore.ts` | Supabase + localStorage. 신청만 건별 insert/delete |
| `src/features/gatherings/` | `GatheringBoard`(피드·상세) · `GatheringForm`(종류 선택 포함) · `PosterFrame` |

**꼭 알아야 할 설계 두 가지**

1. **확정/대기를 저장하지 않는다.** 신청 시각으로 정렬해 앞에서 정원만큼을
   확정으로 *본다*. 그래서 취소는 레코드 삭제 하나로 끝나고 승계가 저절로 된다.
   상태(`모집중`/`마감`)도 같은 이유로 파생시킨다 — 저장하면 아무도 접속 안 하는
   사이 마감 시각이 지나도 '모집중'으로 남는다.
   → **플래그를 저장하고 싶어지면 이 문단을 다시 읽을 것.**
2. **포스터는 그림을 생성하지 않는다.** 틀은 CSS 고정, LLM 은 문구·색·아이콘만.
   이미지 생성 모델을 쓰면 건마다 화풍이 달라 쌓였을 때 오히려 어지럽다.

**아직 안 한 것**

- `supabase-schema.sql` 끝에 `gatherings`, `gathering_signups` 테이블을 넣어뒀다.
  **Supabase 에서 그 SQL 을 실행해야** 기기 간 공유가 된다. 지금은 localStorage 로만 돈다.
- 이미지 첨부는 `gathering-images` 버킷을 쓴다. 버킷을 만들어야 첨부가 저장된다
  (없으면 브라우저 안에서만 보이는 미리보기로 폴백한다).
- 새 모임이 열렸을 때의 **공지 알림은 없다.** 승계·취소만 알린다
  (`notificationRules.gatheringPromotedDraft` / `gatheringCanceledDrafts`).
  전체 공지는 슬랙 소음이 될 수 있어 판단을 남겨뒀다.
- 피드에 **시험용 포스터 7건**이 localStorage 에만 들어 있다(3열 격자 확인용).
  지우려면 브라우저 콘솔에서 `localStorage.removeItem('skgrove:gatherings')`.

### 브라우저 자동화 주의

로그인 폼이 합성 클릭·Enter에 반응하지 않는다(React 상태에 안 닿음). 자동화로
로그인하려면:

```js
document.querySelector('form.login-panel').requestSubmit()
```

입력값도 네이티브 setter + `input` 이벤트로 넣어야 한다. 사람이 직접 타이핑하면 정상.

---

## 바로 할 수 있는 일 (판단 불필요)

### 1. 시드 프로필 성향 값 채우기 — 차트가 4/32라 얇다

`src/data/mockData.ts`의 33명 중 `role`·`collaboration`·`feedback`에 실제 값이 있는
건 **4명뿐**이고 나머지 28명은 `profileFromSeed`의 기본값이다. 그 기본값이 선택지
목록에 없어서 **어느 막대에도 안 잡힌다.**

동료 성향 > 팀 성향 분포가 "다 0 또는 1"인 이유가 이것이다. 선택지는
`src/features/profiles/Profiles.tsx:48~99`의 `roleChoices` / `traitChoices` /
`collaborationChoices` / `feedbackChoices`에 있다. 28명에게 이 중 하나씩 배정하면
분포가 의미를 갖는다.

> 데모용으로는 채우는 게 맞을 수 있지만, 실제 운영이라면 "작성률이 낮다"는 사실
> 자체가 정보다. 지금은 안내문이 `32명 중 4명이 작성`이라고 밝히고 있다.

### 2. 로그인 히어로가 작아졌다

타입 스케일 도입 때 40px가 `--text-display`(28px)로 흡수되면서 헤드라인
"편하게 말하고, 함께 정하고, 조금씩 바꿔요"가 본문과 무게 차이가 줄었다.
이 화면의 주인공이므로 별도 크기를 주는 게 맞다.

### 3. 투표 참여율이 앱에서 사라졌다

홈의 '팀 문화 요약' 패널을 지울 때 함께 없어졌다. `participationRate()`는
`src/agendaRules.ts:30`에 살아 있고 테스트도 있다. 지금은 **어디에도 표시되지
않는다.** 파트지수 화면에 넣을지 판단 필요.

### 4. 내 프로필 카드 4항목의 위계

`역할`(4단어)과 `동료 이해 가이드`(한 문장)가 같은 라벨·같은 크기다. 동료가 이 카드를
볼 때 가장 필요한 건 "이 사람과 **어떻게 일하면 되나**"(협업·피드백·가이드)인데
`역할`과 구분이 없다. 색·간격만으로 조정 가능.

---

## 판단이 필요한 일

### 5. 액션아이템 생성 권한 (별도 작업으로 올려둠)

`src/App.tsx:853`에서 안건 마감은 `canClose={isLeader(currentUser)}`로 막혀 있는데,
같은 곳의 `onCreateActions`에는 권한 검사가 **없다**. `AgendaDetail.tsx:128`의 조건도
`agenda.status === '통과'` 뿐이다.

결과적으로 일반 팀원이 통과된 안건에서 액션아이템을 만들고, `ActionCreateForm.tsx:68`
에서 **아무 활성 계정이나 담당자로 지정**할 수 있다. 참여를 넓히려는 의도일 수도
있어 임의로 바꾸지 않았다.

- 리더 전용이 맞다면 마감과 같은 방식으로 `isLeader` 적용
- 팀원도 허용이 의도라면 담당자를 본인 또는 '미정'으로 제한하는 절충안

### 6. 조뽑기 결과의 개인별 세대 라벨

편집 폼은 "내 카드에도 동료 목록에도 보이지 않아요"라고 약속하는데, 조뽑기 결과에는
실명과 함께 `브릿지`가 붙어 나온다. 조를 짜고 나면 결국 누가 어느 구간인지 드러난다.

완전히 감추려면 조 단위 요약("이 조는 3세대가 섞였어요")만 보여주면 된다. 균형이
맞았다는 정보는 전달되면서 개인 매핑은 안 생긴다.

### 7. 메뉴 이름 — 슬래시 5개와 용어

13개 중 5개가 슬래시 복합명이다: `안건함 / 투표` · `캔미팅 / 티미팅` ·
`커피뽑기 / 조뽑기` · `파트지수 / 리포트` · `알림 / 메시지`.

슬래시는 대개 이름을 못 정했다는 신호다. 스캔할 때 눈이 두 번 멈춘다.

용어도 검토 대상이다. 팀이 이미 쓰는 말이라 합의가 필요하다.

| 현재 | 문제 | 후보 |
|---|---|---|
| 대나무숲 **접수** | 관공서 민원 어감. 심리적 안전을 만드는 화면인데 문턱을 세움 | 말하기 / 의견 보내기 |
| 리더 관리**함** | 사물함 어감 | 리더 검토 |
| **파트지수** | 무엇의 지수인지 없음 | 파트 리포트 |

### 8. 시각적 서명이 없다

moss 계열 319회로 색 운용은 건강한데, 라임을 버린 뒤 그 자리를 채운 게 없다.
잘 만든 사내 도구이되 "SK Grove답다"고 할 요소가 없다.

색 조정으로 되는 게 아니라 **하나의 기억할 만한 요소**(일러스트·마스코트·독특한
그래프 스타일)가 필요한 브랜드 결정이다.

---

## 크지만 급하지 않은 일

### 9. 간격 하드코딩 378건

4px 배수 값은 토큰화했다(718 → 378). 남은 것은 4배수가 아닌 중간값이다:
`10px` 116 · `14px` 72 · `6px` 62 · `18px` 34 · `5px` 20 …

토큰으로 감싸면 글자만 길어지고, 스케일에 맞춰 스냅하면 화면마다 간격이 바뀐다.
**기계적으로 할 일이 아니라 화면별 판단이 필요하다.**

### 10. `rgba()` 95건

대부분 그림자와 반투명 오버레이다. `--shadow-float`로 통합할 수 있는 것과 그렇지
않은 것이 섞여 있다.

### 11. `Meetings.tsx` 1533줄

이번 작업에서 분할하지 않았다. 시각 변경과 파일 분할을 겹치면 회귀 원인을 분리할 수
없기 때문이다. 별도 라운드로 다룰 것.

---

## 작업할 때 유의할 것

### CSS는 소스가 아니라 렌더 결과로 검증한다

이 저장소에서 반복해서 밟은 함정들:

| 함정 | 증상 |
|---|---|
| 미디어쿼리가 기본 규칙보다 **앞**에 있음 | 같은 특이도에서 순서로 짐. 오류 없이 조용히 무시됨 |
| 토큰을 지웠는데 `var()` 참조가 남음 | 오류가 아니라 **빈 값**이 됨. 배경이 투명해지고 그림자가 사라짐 |
| 컴포넌트를 지웠는데 **그리드 열**이 남음 | 화면이 안 깨지고 그냥 좁아짐. 스크린샷으로도 지나침 |
| `:where(...)` 목록 하나가 여러 선택자를 덮음 | grep으로는 1건, 실제로는 68곳. 정적 분석이 과소평가 |
| 자손 선택자(`.list div`)가 남의 컴포넌트를 잡음 | 작성 당시엔 맞았는데 나중에 들어온 컴포넌트를 삼킴 |

**계산된 스타일(`getComputedStyle`)을 확인하기 전에는 "고쳤다"고 말할 수 없다.**

### 검증 방법

브라우저 콘솔에서 대비·타깃·폼 이름·잘림·가로넘침을 한 번에 재는 감사 함수를 쓰면
빠르다. 화면을 순회하며 `getComputedStyle`로 실제 렌더 결과를 계산하는 방식이고,
정적 테스트가 통과한 상태에서 발견된 결함이 대부분 이 방법으로만 드러났다.

두 역할 × 두 폭을 모두 도는 것이 중요하다. **모바일 네비게이션이 아예 없어
720px 이하에서 이동이 불가능했던 결함**이, 1280px에서만 보다가 뒤늦게 발견됐다.

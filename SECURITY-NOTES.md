# 보안 점검 노트

프로토타입 단계에서 의도적으로 열어둔 부분과, 운영 전환 시 반드시 닫아야 하는 부분을 구분해 기록한다.
`dev` 브랜치 기준 점검이며, 코드로 고치지 않고 문서로만 남긴 항목은 이유를 함께 적었다.

## 요약

| # | 항목 | 심각도 | 상태 |
|---|---|---|---|
| 1 | 인증에 비밀번호가 없다 | 높음 | 미조치 (설계 변경 필요) |
| 2 | 모든 테이블 RLS가 전면 허용 | 높음 | 미조치 (1번에 의존) |
| 3 | 투표 익명성이 클라이언트 해시에만 의존 | 중간 | 한계 문서화됨 |
| 4 | 가입 시 본인이 권한을 선택한다 | 낮음 | 미조치 |
| 5 | anon 키가 번들에 포함된다 | 정보 | 정상 (2번이 전제될 때만) |

---

## 1. 인증에 비밀번호가 없다 — 높음

`src/features/auth/LoginScreen.tsx`는 입력된 **이름과 사내메일이 `accounts` 테이블의 행과 일치하는지만** 확인하고 로그인시킨다. 비밀번호도, 메일 인증도, 토큰도 없다.

```
이름 일치 + 이메일 일치 + status가 '활성'  →  로그인 성공
```

`accounts`는 아래 2번 때문에 anon 키로 전체 조회가 가능하므로, **누구나 명단을 받아 임의의 동료로 로그인할 수 있다.** 팀리더 계정도 예외가 아니다.

영향 범위가 넓다. 리더로 로그인하면 대나무숲 접수 내용을 전부 읽을 수 있고, 익명 제보의 보호막이 사실상 사라진다.

**막힌 이유**: 제대로 고치려면 Supabase Auth(매직링크 또는 SSO) 도입이 필요하고, 이는 계정 모델과 로그인 화면을 다시 설계하는 작업이다. 현재 할당된 범위 밖이라 손대지 않았다.

## 2. 모든 테이블 RLS가 전면 허용 — 높음

`supabase-schema.sql`의 모든 정책이 `using (true)` / `with check (true)`다. 대상 테이블은 `accounts`, `issues`, `agendas`, `agenda_ballots`, `action_items` 다섯 개다.

anon 키만 있으면(번들에 포함되어 있으므로 누구나 얻을 수 있다) 다음이 가능하다.

- `accounts` — 팀 전체의 이름·사내메일·권한 조회
- `issues` — **익명으로 접수된 대나무숲 본문 전체 열람**
- `agendas` / `action_items` — 임의 삽입·수정
- `agenda_ballots` — 투표자 키 전수 조회 (3번과 연결)

`issues`는 특히 민감하다. 접수 본문을 저장하도록 최근 바꿨기 때문에(`7f75521`), 이전에는 유실되던 내용이 이제 DB에 남는다. 저장하는 편이 옳지만 **보호 없이 저장하면 유실보다 나쁠 수 있다.**

**막힌 이유**: 정책을 사용자 단위로 좁히려면 `auth.uid()`가 필요하고, 그건 1번이 선행되어야 한다. 인증 없이 RLS만 조이면 앱이 동작하지 않는다. **1번과 2번은 한 묶음으로 처리해야 한다.**

## 3. 투표 익명성이 클라이언트 해시에만 의존 — 중간

`src/ballotStore.ts`의 설계 자체는 익명성을 지키도록 되어 있다. "누가 투표했는가"(`agenda_ballots`)와 "무엇을 골랐는가"(`agendas.approve/reject`)를 분리해, 어느 행도 사람과 선택을 잇지 못한다. 안건마다 다른 `voterKey`를 쓰므로 안건 간 투표 이력도 연결되지 않는다.

한계는 코드 주석에 이미 적어두었고 요약하면 이렇다.

- `voterKey = sha256(안건 id + 소문자 이메일)`은 브라우저에서 계산되므로 비밀이 없다.
- 팀 명단(2번으로 획득 가능)을 알면 후보 메일을 전수 대입해 **특정 안건의 투표 여부**를 밝혀낼 수 있다.
- 다만 **선택(찬성/반대)은 어디에도 신원과 함께 저장되지 않으므로** 그 경우에도 드러나지 않는다.

**개선 방향**: 서버 사이드에서 비밀키를 섞어 서명하면 전수 대입이 막힌다. Supabase Edge Function으로 투표를 받는 구조가 필요하며, 이 역시 1번 이후에 의미가 있다.

## 4. 가입 시 본인이 권한을 선택한다 — 낮음

`LoginScreen`의 가입 폼에서 신청자가 `팀원 / 파트리더 / 팀리더`를 직접 고른다. 상태가 `승인 대기`로 시작하고 팀리더가 활성화해야 하므로 즉시 악용되지는 않지만, 승인자가 권한 항목을 눈여겨보지 않으면 그대로 통과한다.

**개선 방향**: 가입은 항상 `팀원`으로 고정하고, 권한 상향은 계정 관리 화면에서만 하도록 분리한다. 작은 변경이라 인증 개편과 별개로 처리 가능하다.

## 5. anon 키가 번들에 포함된다 — 정보

`VITE_` 접두사 환경변수는 클라이언트 번들에 그대로 들어간다. 이것은 Supabase anon 키의 정상적인 사용법이며 그 자체로는 문제가 아니다. **단, anon 키는 RLS가 방어선이라는 전제 위에서만 안전하다.** 2번이 열려 있는 한 이 키는 사실상 전체 DB 접근 권한이다.

`.env`는 `.gitignore`에 있고 저장소에 커밋된 시크릿은 없다. (`.env.example`만 추적됨)

---

## 권장 처리 순서

1. **인증 도입** (1번) — Supabase Auth. 다른 모든 항목의 선행 조건이다.
2. **RLS 재작성** (2번) — `auth.uid()` 기반. 1번과 같은 PR로 처리해야 중간 상태가 깨지지 않는다.
3. **가입 권한 고정** (4번) — 위 둘과 독립적으로 먼저 해도 된다.
4. **투표 서명 서버화** (3번) — 익명성 보장 수준을 올려야 한다면.

프로토타입으로 계속 쓸 것이라면 1·2번을 미루는 선택도 가능하다. 다만 그 경우 **실제 팀원에게 익명 제보를 받는 용도로는 쓰면 안 된다.** 지금 구조에서 익명성은 화면 표시 규칙일 뿐 데이터 보호가 아니다.
